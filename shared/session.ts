/**
 * A readable, non-authorizing session marker. It selects account-scoped browser
 * caches; the HttpOnly session cookie remains the only credential.
 */
export const SESSION_OWNER_COOKIE = "mh_session_owner";
